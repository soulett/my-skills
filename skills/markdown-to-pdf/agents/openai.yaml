interface:
  display_name: "Markdown to PDF"
  short_description: "Turn Markdown into clean PDFs with local tooling."
  default_prompt: "Use $markdown-to-pdf to convert one or more Markdown files into polished PDFs."

policy:
  allow_implicit_invocation: true
